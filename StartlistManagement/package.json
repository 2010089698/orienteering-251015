{
  "name": "startlist-management",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/backend"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:frontend": "npm --prefix .. run test:run --workspace @orienteering/startlist-frontend",
    "dev:backend": "npm run start --workspace @startlist-management/backend"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^1.6.0"
  }
}